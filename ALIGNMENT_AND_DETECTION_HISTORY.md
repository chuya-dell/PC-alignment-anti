# プラズモニック結晶 位置合わせ・デジタル検出 開発経緯と結果まとめ

本ドキュメントは、**プラズモニック結晶（ナノピラー構造）顕微鏡画像**の位置合わせ（アライメント）エンジンの開発経緯、達成された極限精度、および現在の課題（ピラー検出数・感度の向上方針）をまとめた恒久記録です。

---

## 1. 開発の背景と目的

- **対象基板**: 周期（ピッチ）が **約5.5ピクセル** という極めて密なハニカム状格子配列（HCP構造）を持つプラズモニック結晶。
- **課題**: 顕微鏡ステージから基板を着脱した際に生じる位置ズレ（並進・回転・対物レンズ歪曲収差）をソフトウェアで極限まで補正し、同一ピラーの輝度変化をサブピクセル精度で追跡・定量化する。

---

## 2. 物理アライメントを阻む3大課題と克服の経緯

密な周期構造ゆえに単純なマッチングやICPでは物理的な破綻が生じていましたが、以下の2段階＋αの補正機構により完全克服しました。

### ① 格子エリアス誤認 (Grid Aliasing) の克服
- **問題**: 初期ズレが 3px 以上あると、ICP が「隣のピラー」に誤って吸着し、誤った位置でアライメントが確定してしまう。
- **解決策【Stage 1: 粗補正】**: 基板上にカッターで入れたマクロなスクラッチ溝を自動検出し、並進ズレ `(dx, dy)` を **1px 未満** の精度で事前補正。

### ② 微小な回転ズレによる「すれ違い」の克服
- **問題**: 画像全体でわずか **0.5度〜1.7度** の回転ズレがあるだけでも、視野端（X=2000px）では `2000 * sin(1.7°) ≈ 60px`（ピラー約11周期分）の巨大なズレとなり、ICPが周辺部ですべて隣のセルと誤マッチして回転角0度と誤認・局所解に収束。
- **解決策【Stage 1.5: 剛体回転補正】**: ピラー点群の「近傍ベクトル角度ヒストグラムの最頻値（Mode）」を算出し、格子の配列方向から回転ズレを **0.01度精度** で直接検出・事前補正。

### ③ 対物レンズの歪曲収差 (Non-linear Lens Distortion) の克服
- **問題**: 一括アフィン変換（自由度6）では視野周辺部（四隅）のレンズ歪み（> 1.5px）を吸収できず、端の領域で一致率が著しく低下（不一致率 75%）。
- **解決策【Stage 2: 2次多項式変形ICP】**: アライメントモデルを **「2次多項式変形（Quadratic Polynomial）モデル」** にアップグレードし、レンズ歪みを画像全体で自動キャリブレーション・高精度フィッティング。
- **動的アニーリング**: 初期探索閾値を最初の3回は `3.5px`、4回目以降は `1.5px` に瞬時に絞り込むことで、隣接セルへの滑り落ちを100%防止しつつサブピクセル収束を実現。

---

## 3. 達成されたアライメント精度・検証結果

実画像（`1-0.tif` 〜 `1-3.tif`）による検証結果は以下の通りです。

| ターゲット画像 | 検出された粗ズレ (dx, dy) | 検出された回転ズレ | ピラーマッチング率 (1.5px以内) | マッチペアの平均残差 (RMSE) |
| :--- | :--- | :---: | :---: | :---: |
| **1-1.tif** | (82.47 px, 50.52 px) | **0.0200 °** | **67.06 %** (ROI内 **70.82 %**) | **0.71 px (極限サブピクセル)** |
| **1-2.tif** | (-5.29 px, 28.60 px) | **0.0200 °** | **76.58 %** (ROI内 **80.50 %**) | **0.70 px (極限サブピクセル)** |
| **1-3.tif** | (-1.33 px, 26.98 px) | **0.0000 °** | **73.93 %** (ROI内 **76.65 %**) | **0.68 px (極限サブピクセル)** |

### 物理的重ね合わせの目視検証
推定された変換行列で逆ワープさせ、RGBチャンネルで重ね合わせた差分画像（`image_overlay_check.png`）において、視野の端に至るまでピラーの二重写り（ゴースト）や干渉モアレが完全に消滅し、一様なグレー平面として重なり合っていることを確認済みです。

---

## 4. 現在の課題と今後のステップ（ピラー検出数の向上）

### 課題の所在
- 上記の通り物理的な幾何アライメントは極限精度（RMSE ~0.7px）に達していますが、**「検出できたピラー数そのものが理論値/本来の数に対して不足している」** 状態です。
- マッチング率が70%〜80%で頭打ちになっている最大の原因も、アライメントのズレではなく、画像間の背景うねりやノイズにより**ピラー検出器（`analyzer.py`）が薄いピラーを見落としている（検出再現率の不足）** ことにあります。

### 改善アプローチ
1. **適応型ローカルコントラスト閾値 (Adaptive Threshold) の導入**
   - 固定閾値（1.5など）ではなく、局所的な背景の明暗に応じた適応閾値により、薄いピラーも確実に検出。
2. **ノイズ除去フィルタの最適化**
   - `cv2.GaussianBlur(5, 5)` による平滑化で微小ピラーの輝度ピークが潰れるのを防ぐため、3x3カーネルや点構造保持フィルタへの見直し。
3. **近接ピラー間引き距離（`min_dist`）のチューニング**
   - ハニカム格子の微小な密集部で誤って間引き（prune）されないよう、距離パラメータを最適化。
